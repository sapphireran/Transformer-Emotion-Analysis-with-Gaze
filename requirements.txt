# Dependencies for the personal docs/examples tooling in this repo.
# The original training scripts also need the extras in requirements-train.txt.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pytest>=8.0
