# Lightweight stack for the documentation examples under examples/.
# These packages are enough to inspect the checked-in CSVs, compute
# gaze-feature reports, and run the toy late-fusion demo.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
