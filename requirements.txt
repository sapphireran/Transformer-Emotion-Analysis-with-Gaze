# Full training stack used by model_full_SST.py and model_ZuCo_SST.py.
# The examples/ scripts only need numpy (already enough for the docs demos).

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
torch>=2.0
transformers>=4.36
datasets>=2.16
tqdm>=4.66
nltk>=3.8
