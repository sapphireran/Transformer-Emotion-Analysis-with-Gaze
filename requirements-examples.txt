# Lightweight stack for docs/examples and their tests.
# Training scripts (model_*.py) additionally need torch, transformers, datasets, tqdm.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
pytest>=8.0
